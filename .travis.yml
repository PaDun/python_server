language: python
python: "2.7"
install:
  - pip install codecov
  - pip install -r requirements.txt
script:
  - pytest
  - pytest --cov=src tests/
  - coverage run tests.py
after_success:
  - codeclimate-test-reporter
  - codecov
